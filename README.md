# WB Tariffs Service

Сервис для автоматического сбора и хранения тарифов Wildberries с возможностью экспорта в Google Sheets.

## 📋 Возможности

- Ежечасный сбор тарифов из API Wildberries
- Сохранение истории тарифов в PostgreSQL
- Автоматический экспорт в Google Sheets (опционально)
- Запуск в Docker контейнерах
- Поддержка нескольких Google таблиц

## 🚀 Быстрый старт

### 1. Клонировать репозиторий
git clone https://github.com/your-username/wb-service.git
cd wb-service

### 2. Создать файл с переменными окружения
cp .env.example .env

Установить нужные переменные окружения

### 3. Запуск
docker-compose up


### Для проверки работы есть папка logs
Без реальных переменных создастся база даннных и и запустится приложение, но данные не придут, не сохранятся в бд и не экспортируются в google таблицу